{
  "name": "deepwiki-generator",
  "displayName": "DeepWiki Generator",
  "description": "Generate comprehensive DeepWiki documentation using autonomous AI agents.",
  "version": "0.2.1",
  "publisher": "your-publisher",
  "engines": {
    "vscode": "^1.95.0"
  },
  "dependencies": {},
  "categories": [
    "AI",
    "Other"
  ],
  "activationEvents": [],
  "main": "./out/extension.js",
  "contributes": {
    "configuration": {
      "title": "DeepWiki Generator",
      "properties": {}
    },
    "languageModelTools": [
      {
        "name": "deepwiki-generator_createDeepWiki",
        "tags": [
          "documentation",
          "wiki",
          "deepwiki",
          "workspace"
        ],
        "toolReferenceName": "createDeepWiki",
        "displayName": "Create DeepWiki",
        "modelDescription": "Analyzes the current workspace and generates comprehensive DeepWiki documentation using autonomous sub-agents. It orchestrates a Surveyor, Analyst, and Writer agent to plan, analyze, and document the codebase.",
        "userDescription": "Generate DeepWiki documentation for the current workspace.",
        "canBeReferencedInPrompt": true,
        "icon": "$(book)",
        "inputSchema": {
          "type": "object",
          "properties": {
            "outputPath": {
              "type": "string",
              "description": "Optional output directory path for the generated DeepWiki. Defaults to '.deepwiki' in workspace root."
            }
          }
        }
      }
    ]
  },
  "scripts": {
    "vscode:prepublish": "npm run compile",
    "compile": "tsc -p ./",
    "build": "npm run compile && vsce package --allow-missing-repository --skip-license",
    "watch": "tsc -watch -p ./",
    "lint": "eslint src --ext ts"
  },
  "devDependencies": {
    "@types/node": "^20.0.0",
    "@types/vscode": "^1.95.0",
    "@typescript-eslint/eslint-plugin": "^7.0.0",
    "@typescript-eslint/parser": "^7.0.0",
    "@vscode/vsce": "^3.0.0",
    "eslint": "^8.56.0",
    "typescript": "^5.3.0"
  }
}